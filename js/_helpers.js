// #tagCloud on Design Work

// Get all tag contents on page
var tag = $('.tag').val();

// Make an array of tag ids
    // Only take unique tags (no duplicates)
// Populate a list of tags in tag cloud

// When clicking on a tag
    // hide all sections that don't have that tag as a class
    // show all sections that were hidden that do have that tag as a class
